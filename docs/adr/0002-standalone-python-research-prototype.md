# Keep the adaptive-learning research prototype as a standalone Python module

The adaptive-learning thesis prototype will live as an independently executable Python research module inside the repository rather than being integrated into the React frontend or Node/Express product backend. This keeps synthetic dataset generation, machine-learning experiments, statistical analysis, visualizations, and thesis reporting reproducible for academic work while allowing the product runtime to consume selected outputs later if needed.
