# Use a local Node/Express API with SQLite for the first real backend

Lingoix will first move from in-code synthetic data to a local Node/Express API backed by SQLite. This gives the project real persistence, authentication, role-based access, editable admin workflows, learning-event collection, and adaptive-rule execution without requiring cloud infrastructure yet; SQLite can later be migrated to Postgres when deployment scale or research data volume requires it.
